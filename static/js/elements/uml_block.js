// Generated by CoffeeScript 1.9.1
(function() {
  var extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  this.UMLBlock = (function(superClass) {
    extend(UMLBlock, superClass);

    function UMLBlock(svg, ClassName, x, y, width, height, fields, methods) {
      var head, header_height, header_size, name, name_size, startx, starty, uml_block;
      header_height = height * 0.25;
      head = svg.rect(x, y, width, header_height);
      head.addClass('uml_block__head');
      header_size = head.getBBox();
      name = svg.text(x, y, ClassName);
      name_size = name.getBBox();
      name.attr({
        x: x + header_size.width / 2 - name_size.width / 2,
        y: y + name_size.height + header_size.height / 2 - name_size.height / 2
      });
      uml_block = svg.group(head, name);
      uml_block.addClass('uml_block');
      startx = void 0;
      starty = void 0;
      uml_block.drag();
      eve.on("snap.drag.move." + uml_block.id, function() {
        return console.log(uml_block.getBBox());
      });
    }

    return UMLBlock;

  })(EventEmitter);

}).call(this);

//# sourceMappingURL=uml_block.js.map
